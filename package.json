{
  "name": "vintage-video",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "The Reel Vault — a fully client-side Astro app for streaming vintage public-domain movies and videos from the Internet Archive.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "astro": "^4.16.18",
    "wrangler": "^4.95.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "11"
  }
}
